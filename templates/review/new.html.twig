{% extends "base.html.twig" %}

{% block body %}
<div class="container">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Ajouter une critique</button>
    {{ form_end(form) }}
</div>
{% endblock %}